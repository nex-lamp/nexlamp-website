# 2026-08-13 工作日志

## nexlamp-publisher 全平台发布（第二轮，手动触发）

- **选题**：LED天空灯首部团体标准 T/CALI 0403-2026（中照协 8月6日发布，佛山照明牵头）
  - A/B/C 三级分类 + 9大核心指标；C类要求 1800K-12000K 连续可调
  - 驱动视角三道门槛：超宽色温多通道混光 / 瑞利散射双模块并行 / AI场景控制
- **文章**：`posts/2026-08-13-led-sky-light-standard-t-cali-0403-2026.md`（~1500字，第138篇）
- **配图**：3 张深色科技风 SVG + 1 张 PIL 合成封面
  - `images/blog-sky-light-standard-2026-classification.svg`
  - `images/blog-sky-light-standard-2026-cct-range.svg`
  - `images/blog-sky-light-standard-2026-dual-module.svg`
  - `images/blog-sky-light-standard-2026-cover.png`
- **发布结果**：网站 ✅（commit 1873b3f 已 push）| Dev.to ✅ id=4384075 | 今日头条 ✅ | LinkedIn ⏭️ 跳过（CDP未运行）

## 重要教训与新发现

1. **选题去重必须前置**：今天 12:00 定时任务已发过一篇「无网关自组网」，本轮初版又撞上「全光谱人因照明」（与 6/24、7/31、8/5 三篇重复），白写一篇 + 4 张图。以后写之前先 `ls posts/ | grep -iE "关键词"`。
2. **ImageGen 429 额度耗尽**（区别于此前的 150 任务上限）：改用系统 Python PIL 程序化合成封面，效果可接受且省掉去水印步骤。系统 Python 路径 `C:\Users\yu\AppData\Local\Programs\Python\Python313\python.exe` 有 PIL，managed Python 没有。
3. **claude-vision 百炼 API key 已失效**（401 Incorrect API key）→ 需要老刘更新 `~/.workbuddy/skills/claude-vision/scripts/vision.js` 中的 key，否则读图能力不可用。
4. **git push 网络抖动**：连续 3 次 Connection reset / 21s 超时，第 4 次自然恢复。不必急着走 GitHub Contents API 回退，先重试几次。
5. **Dev.to** 本次 POST 直接拿到正式 slug（无 temp-slug）；`published` 字段返回 None 是 API 特性，用 `curl -o /dev/null -w "%{http_code}"` 访问文章页确认 200 即可判定已发布。
